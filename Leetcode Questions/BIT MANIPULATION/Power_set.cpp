#include<iostream>
#include <vector>
using namespace std;
void powerset(vector<int>&arr){
    int n = arr.size();
    int total = 1<<n; // 2^n subsets 
    for(int mask = 0; mask<total;mask++){
        if(mask & 1){

        }
    }

    
}
int main(){

return 0;
}